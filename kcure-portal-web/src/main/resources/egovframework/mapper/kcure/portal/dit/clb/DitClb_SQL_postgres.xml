<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ditClb">
	<select id="selectCrcnNmList" parameterType="egovMap" resultType="egovMap">
	   SELECT DISTINCT(CRCN_NM) AS CRCN_NM
	  		, DTS_LCLS_CD
	   FROM KCURE_COM_TBL_DFND
	   WHERE DTS_LCLS_CD IN ('01','02')
	</select>

	<select id="selectLclsNmList" parameterType="egovMap" resultType="egovMap">
		SELECT A.LCLS_NM
		FROM
			(SELECT LCLS_NM, MAX(TBL_ORD) tbl_ord
			FROM KCURE_COM_TBL_DFND
			WHERE DTS_LCLS_CD = #{dtsLclsCd}
			GROUP BY  LCLS_NM
			)A
		ORDER BY TBL_ORD
	</select>

	<select id="selectMclsNmList" parameterType="egovMap" resultType="egovMap">
		SELECT A.MCLS_NM, A.TBL_ENM
		FROM
			(SELECT MCLS_NM, MAX(TBL_ORD) TBL_ORD, MAX(TBL_ENM) AS TBL_ENM
			FROM KCURE_COM_TBL_DFND
			WHERE DTS_LCLS_CD = #{dtsLclsCd}
			AND LCLS_NM = #{lclsNm}
			GROUP BY  MCLS_NM
			)A
		ORDER BY TBL_ORD
	</select>

	<select id="selectColKnmList" parameterType="egovMap" resultType="egovMap">
		SELECT COL_KNM
		FROM KCURE_COM_COL_DFND
		WHERE TBL_ENM = #{tblEnm}
		ORDER BY COL_ORD
	</select>
</mapper>